version: '3.8'

services:
  ringatrade-web:
    build: .
    container_name: ringatrade-web
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
    env_file:
      - .env
    volumes:
      - ./src/data:/app/src/data
